#include <iostream>
#include <vector>

int main()
{
    int kettlebellMax{};
    std::vector<int> set;
    std::vector<int> left;
    std::vector<int> right;

    int weight;

    std::cout << "Input weight: ";
    std::cin >> weight;

    bool isFind{ false };
    while(true)
    {
        
        
    }
}
